{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "node",
			"dockerBuild": {
				"dockerfile": "${workspaceFolder}/ciclo4_Micro_sale/Dockerfile",
				"context": "${workspaceFolder}/ciclo4_Micro_sale",
				"pull": true
			},
			"node": {
				"package": "${workspaceFolder}/ciclo4_Micro_sale/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: release",
			"dependsOn": [
				"docker-build"
			],
			"platform": "node",
			"node": {
				"package": "${workspaceFolder}/ciclo4_Micro_sale/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"env": {
					"DEBUG": "*",
					"NODE_ENV": "development"
				}
			},
			"node": {
				"package": "${workspaceFolder}/ciclo4_Micro_sale/package.json",
				"enableDebugging": true
			}
		}
	]
}